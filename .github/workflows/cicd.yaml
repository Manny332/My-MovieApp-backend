name: Build and push


on:
  push:
    branches: [main]

jobs:
  build-and push:
    runs-on: ubuntu-latest

    steps:
    - name: checkout the repository
      use: actions/checkout@v2

    - name: Docker build
      run: |
        hostnamectl   