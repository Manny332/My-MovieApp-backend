name: Build and push


on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: checkout the repository
      uses: actions/checkout@v2

    - name: Docker build
      run: |
        hostnamectl   

jobs:
  build-and-push2:
    runs-on: ubuntu-latest

    steps:
    - name: checkout the repository
      uses: actions/checkout@v2

    - name: Docker build2
      run: |
        hostnamectl   

jobs:
  build-and-push3:
    runs-on: ubuntu-latest

    steps:
    - name: checkout the repository
      uses: actions/checkout@v2

    - name: Docker build3
      run: |
        hostnamectl                   