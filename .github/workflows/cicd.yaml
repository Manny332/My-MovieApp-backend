name: Build and push


on:
  push:
    branches: [main]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: checkout the repository
      uses: actions/checkout@v2

    - name: Docker build
      run: |  
        Docker build -t My-MovieApp-backend

 