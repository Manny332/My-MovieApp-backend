name: Build and push


on:
  push:
    branches: [main]

jobs:
  build-and-push-1:
    runs-on: ubuntu-latest

    steps:
    - name: checkout the repository
      uses: actions/checkout@v2

    - name: Docker build-1
      run: |
        hostnamectl   


  build-and-push-2:
    runs-on: ubuntu-latest
    needs: build-and-push-1

    steps:
    - name: checkout the repository
      uses: actions/checkout@v2

    - name: Docker build-2
      run: |
        hostnamectl   


  build-and-push-3:
    runs-on: ubuntu-latest
    needs: build-and-push-1

    steps:
    - name: checkout the repository
      uses: actions/checkout@v2

    - name: Docker build-3
      run: |
        hostnamectl                   


  build-and-push-4:
    runs-on: ubuntu-latest
    needs: build-and-push-3

    steps:
    - name: checkout the repository
      uses: actions/checkout@v2

    - name: Docker build-4
      run: |
        hostnamectl                   


  build-and-push-5:
    runs-on: ubuntu-latest
    needs: build-and-push-4

    steps:
    - name: checkout the repository
      uses: actions/checkout@v2

    - name: Docker build-5
      run: |
        hostnamectl                   


  build-and-push-6:
    runs-on: ubuntu-latest
    needs: build-and-push-4

    steps:
    - name: checkout the repository
      uses: actions/checkout@v2

    - name: Docker build-6
      run: |
        hostnamectl                   


  build-and-push-7:
    runs-on: ubuntu-latest
    needs: build-and-push-5

    steps:
    - name: checkout the repository
      uses: actions/checkout@v2

    - name: Docker build-7
      run: |
        hostnamectl                   